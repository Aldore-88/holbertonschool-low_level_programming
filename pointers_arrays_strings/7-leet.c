#include "main.h"
/**
 *
 *
 *
 *
 */
char *leet(char *str)
{
	int i = 0;

	while (str[i] != '\0')
	{
		if(str[i] = 'a' || str[i] = 'A')
			str[i] = '4';

		if(str[i] = 'e' || str[i] = 'E')
			str[i] = '3';

if 
	}
}
